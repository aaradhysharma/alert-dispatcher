# Copy to `.env` and replace with local / sandbox values. Do not commit `.env`.
#
#   cp .env.example .env

MOCK_EMAIL_API_KEY=sk-replace-with-your-dev-or-mock-key
MOCK_SLACK_WEBHOOK=https://hooks.slack.com/services/REPLACE/WITH/YOUR/WEBHOOK

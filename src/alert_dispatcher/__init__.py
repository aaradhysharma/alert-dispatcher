"""Alert Dispatcher.

Runtime version is bumped here as the refactor lands. The package
distribution version in `pyproject.toml` is intentionally left at
0.1.0 (that file is locked per project standards), so for now the
two are allowed to diverge until the next coordinated release.
"""

__version__ = "0.2.0"
